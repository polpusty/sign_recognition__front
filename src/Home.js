import React, {Component} from 'react';
import Loading from "./Loading";

class Home extends Component {
    render() {
        return (
            <Loading loading={true}><p>d</p></Loading>
        );
    }
}

export default Home;
